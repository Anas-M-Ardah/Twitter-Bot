<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Twitter Bot</title>
</head>
<body>
    <h1>Welcome to the Twitter Bot</h1>
    <p>This is the landing page for the Twitter Bot project.</p>
    <p>The scheduled tweet function runs automatically based on the configured schedule.</p>
    
    <!-- Password Form -->
    <form action="/" method="POST">
        <label for="password">Enter Password to Tweet:</label>
        <input type="password" name="password" id="password" required>
        <button type="submit">Submit</button>
    </form>

    <!-- Display Tweet Status Message -->
    <% if (message) { %>
        <p style="color: <%= message === 'Tweet posted successfully' ? 'green' : 'red' %>;"><%= message %></p>
    <% } %>
</body>
</html>
